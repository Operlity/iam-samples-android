<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AuthApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecureAuthApp"
        android:networkSecurityConfig="@xml/network_security_config">
        
        <!-- Splash Screen - Entry Point -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SecureAuthApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Screen -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:launchMode="singleTask" />

        <!-- Home Screen -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- Profile Screen -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!-- Contact Management -->
        <activity
            android:name=".ui.contact.ContactListActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.contact.AddEditContactActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Legacy MainActivity (keeping for reference) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <!-- AppAuth redirect callback activity -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.example.securevault"
                    android:host="callback" />
            </intent-filter>
        </activity>
    </application>

</manifest>
